# ============================================================================
# DATABASE VARIATION - SQLITE
# ============================================================================
# Perfect for: Development, testing, small deployments, embedded apps
# Handles: Up to 100,000 requests/day
# Storage: Single file database (easy backup)
#
# Advantages:
#   ✅ Zero setup - just works
#   ✅ No external dependencies
#   ✅ Easy backup (copy .db file)
#   ✅ Works offline
#   ✅ Perfect for edge deployments
#
# Limitations:
#   ⚠️ Single writer at a time
#   ⚠️ No built-in replication
#   ⚠️ Limited to ~100k requests/day
#
# When to upgrade:
#   - More than 100k requests/day → Use PostgreSQL
#   - Need replication → Use PostgreSQL
#   - Need real-time features → Use Supabase
# ============================================================================

DEPLOYMENT_MODE=single-vm
NODE_ENV=production

# Database - SQLite
DATABASE_TYPE=sqlite
DATABASE_PATH=./data/bharatmart.db
SQLITE_TIMEOUT=5000
SQLITE_BUSY_TIMEOUT=3000

# Enable WAL mode for better concurrency
SQLITE_WAL_MODE=true

# Backup configuration
SQLITE_BACKUP_PATH=./backups
SQLITE_BACKUP_INTERVAL=86400  # Daily backups

# Authentication - Local JWT (SQLite has no built-in auth)
AUTH_PROVIDER=local
JWT_SECRET=<REPLACE_WITH_SECURE_RANDOM_64_CHAR_STRING>
JWT_EXPIRY=1h
JWT_REFRESH_EXPIRY=7d

# Other layers (choose based on needs)
WORKER_MODE=in-process
CACHE_TYPE=memory

PORT=3000
FRONTEND_URL=http://localhost:3000

# Migration from SQLite to PostgreSQL:
# 1. Export data: sqlite3 bharatmart.db .dump > backup.sql
# 2. Convert to PostgreSQL format (update syntax)
# 3. Import to PostgreSQL: psql bharatmart < backup.sql
# 4. Update DATABASE_TYPE=postgresql
