# ============================================================================
# LOCAL DEVELOPMENT - MINIMAL (Zero Dependencies)
# ============================================================================
# Perfect for: Learning, quick testing, offline development
# Setup time: 1 minute
# Monthly cost: $0
# Internet required: No
# External services: None
#
# Usage:
#   1. cp config/samples/local-dev-minimal.env .env
#   2. npm install
#   3. npm run dev (frontend) + npm run dev:server (backend)
#
# Guide: DEPLOYMENT_QUICKSTART.md#scenario-1-local-development
# ============================================================================

# Deployment Configuration
DEPLOYMENT_MODE=single-vm
NODE_ENV=development

# Database - SQLite (file-based, zero setup)
DATABASE_TYPE=sqlite
DATABASE_PATH=./bharatmart.db

# Authentication - Local JWT
JWT_SECRET=change-this-to-a-secure-random-string-min-32-chars
JWT_EXPIRY=1h
JWT_REFRESH_EXPIRY=7d

# Workers - In-process (no external queue)
WORKER_MODE=in-process
WORKER_CONCURRENCY=2

# Cache - Memory (no persistence)
CACHE_TYPE=memory
CACHE_TTL=300

# Secrets Management
SECRETS_PROVIDER=env

# Server Configuration
PORT=3000
HOST=localhost
FRONTEND_URL=http://localhost:5173

# Logging
LOG_LEVEL=debug
LOG_FORMAT=dev

# Frontend Configuration (for Vite)
VITE_API_URL=http://localhost:3000
VITE_APP_NAME=BharatMart
